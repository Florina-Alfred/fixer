name: Awk Sales Reports
image: python:3.11
category: awk
level: intermediate
goal: "Calculate reports using awk"
description: |
  ## Scenario
  Calculate sales reports using awk.
  
  File: /tmp/labs/sales.csv
  
  ## Tasks
  1. Calculate revenue → /tmp/labs/revenue.txt
  2. Calculate totals by status → /tmp/labs/orders-by-status.txt
  
  ## Validation
  Run `v` to check

setup:
  - 'python3 /setup/setup.py'
validate:
  - 'python3 /validate/validate.py'
hints:
  - 'awk -F"|" "{print $3}" /tmp/labs/sales.csv'