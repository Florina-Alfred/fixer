# Play 1: Find All Configuration Files
#
# Task: Find all .yml and .conf files in /app and save the list to /tmp/config-files.txt
#
# Hint: Use find with -name pattern matching
# Hint: You may need to use -o (OR) to match multiple patterns
#
# Expected: At least 2 files found
