# Play 2: Find Hidden Files
#
# Task: Find all hidden files (starting with .) in /app and its subdirectories
# Save the list to /tmp/hidden-files.txt
#
# Hint: Hidden files start with a dot
# Hint: Use -name ".*" pattern
#
# Expected: At least 3 hidden files/directories
