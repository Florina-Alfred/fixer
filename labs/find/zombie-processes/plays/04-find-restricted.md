# Play 4: Find Restricted Files
#
# Task: Find all files in /app that have restricted permissions (600 or less)
# These are files that only the owner can read/write
# Save the list to /tmp/restricted-files.txt
#
# Hint: Use -perm to filter by permissions
# Hint: Use -perm -600 or -perm 600
#
# Expected: At least 1 restricted file (api_keys.conf)
