# Play 4: Extract Network Traffic Data
#
# Task: From /data/network.log, extract source IP (before the colon) and destination IP
# Save to /tmp/network-traffic.txt
# Format: SRC_IP|DST_IP
#
# Hint: The format is: SRC_IP:PORT -> DST_IP:PORT | PROTOCOL | BYTES | STATUS
# Hint: You may need to combine cut with other tools, or use field ranges
#
# Expected: 10 traffic entries
